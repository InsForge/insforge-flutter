## 0.2.0

- Depends on `insforge ^0.2.0`, which adds passwordless email OTP sign-in
  (`signInWithOtp` / `verifyOtp`) and batch object deletion
  (`deleteAll` → `List<DeleteObjectResult>`).

## 0.1.0

Initial release.
