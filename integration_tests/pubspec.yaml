# integration_tests/pubspec.yaml
name: insforge_integration_tests
description: Live-backend integration test suite for the InsForge Flutter SDK.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.7.0
  insforge: ^0.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
